<div>
  <h1>Add a new bookmark</h1>
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%= f.submit %>
  <% end %>
</div>

<div>
  <p><%= link_to "Back to #{@list.name}", list_path(@list) %></p>
</div>
